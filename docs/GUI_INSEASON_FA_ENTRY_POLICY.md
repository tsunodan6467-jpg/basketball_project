# 国内バスケGM開発 GUI インシーズンFA / 契約導線方針メモ

**作成日**: 2026-04-07  
**文書の性質**: **小設計メモ**。インシーズンFA・シーズン中の「プールからの契約」まわりの **現状事実**と、**GUI 化するなら最小単位**だけを固定する。完成仕様・`free_agent_market` 改修の正本ではない。

| 参照 | 文書 |
|------|------|
| FA / 契約全体の入口方針（オフ再契約含む） | `docs/GUI_FA_CONTRACT_ENTRY_POLICY.md` |
| GUI 主導線のギャップ | `docs/GUI_MAIN_FLOW_AUDIT.md` |
| 現状ラベル | `docs/CURRENT_STATE_ANALYSIS_MASTER.md` |
| 実装順の正本 | `docs/IMPLEMENTATION_PLAN_MASTER.md` |
| 人事・編成の理想例示 | `docs/IDEAL_GAME_DESIGN_MASTER.md` |
| トレード最小導線（比較） | `docs/GUI_ONE_FOR_ONE_TRADE_ENTRY_POLICY.md` |

**コード上の事実（リポジトリ静的確認・2026-04-07）**: `main.py` の `run_gm_menu` に **FA プールから選手を選んで契約するメニュー項目はない**。`sign_free_agent` の呼び出しは **`free_agent_market.run_cpu_fa_market_cycle` 内**（＋テスト）に限定される。シーズンシミュ中は `Season._process_inseason_free_agency` → `run_cpu_fa_market_cycle` で **CPU チーム**が補強しうる。人事 GUI の **契約＋1年**・**解除（FA 送り）**・**1対1トレード**は別経路。

---

## 0. この文書の使い方

- **何のためか**: **オフ再契約 GUI 済み**の次候補として「インシーズンFA / 契約」を語る前に、**いまプレイヤーが何をできて何をできないか**を事実で揃え、**最小の GUI スコープ**を1つに絞る。
- **コード変更ではない**: 本書のみ。ウィンドウ追加は別タスク。
- **役割分担**: オフ再契約・`resign_ui_prompt` は **`GUI_FA_CONTRACT_ENTRY_POLICY.md`**。本書は **レギュラー中の FA プール契約（ユーザー操作）**に限定する。

---

## 1. 現在のインシーズンFA / 契約の位置づけ

| 論点 | 内容 |
|------|------|
| **CLI で「FA プールから契約」** | **`run_gm_menu` に該当項目なし**（静的確認）。トレードは **10** のみ（`run_trade_menu`）。multi は **チーム間＋現金**であり、**FA 一覧からの指名ではない**。 |
| **GUI でシーズン中の契約** | **契約＋1年延長**・**契約解除（FA 送り）**・**1対1トレード**は人事にあり。**FA プールからの新規獲得 UI はなし**。 |
| **CPU インシーズンFA** | 各ラウンド処理の一部で `Season._process_inseason_free_agency` → `run_cpu_fa_market_cycle`。`cpu_inseason_fa_allowed_for_simulated_round` により **ラウンド22超過後は空振り**（`season_transaction_rules` と整合）。 |
| **`sign_free_agent` の利用箇所** | **`run_cpu_fa_market_cycle` 内**と **pytest**（`test_economy_r1_fa_payroll_trace` 等）。**ユーザー対話コードからは呼ばれていない**（静的確認）。 |
| **未確認** | リポジトリ外ブランチ・ローカル未コミットの有無。**全エントリポイントの動的網羅**（手動プレイ全分岐）は未実施。 |
| **文言上のギャップ** | `main_menu_view._format_hr_trade_fa_guidance_text` は **「インシーズンFA の本操作は CLI が正本」** と書くが、**対応する CLI メニューは `run_gm_menu` に見当たらない**。→ **案内と実装の不一致**（本メモでは断定せず **要修正候補**として扱う）。 |

**オフ再契約**: 本書の主題外（既に GUI / `resign_ui_prompt` でカバー）。

---

## 2. 現状の経路確認

| 問い | 整理結果 |
|------|----------|
| **ユーザーがインシーズンFAを自分で選んで契約できる経路があるか** | **リポジトリ内の `main.py` ユーザー向けメニューでは見つからない**（**未実装と判断**）。 |
| **あるならどこか** | **該当なし**（上記確認範囲）。 |
| **ないなら近い既存経路** | **CPU 自動補強**（シミュ進行に紐づく）。**人事の解除**で選手を FA プールへ送ることはできるが、**プールからの逆方向（獲得）は別操作**。 |
| **CPU 補強の経路** | `Season` ラウンド進行 → `_process_inseason_free_agency` → `run_cpu_fa_market_cycle`（全 `all_teams`、ユーザーチームも対象になりうる）。 |
| **`sign_free_agent` が使われる層** | **`free_agent_market`（CPU サイクル）** と **テスト**。 |

---

## 3. GUI 入口の候補

| 案 | メリット | デメリット | 分かりやすさ | 構造への乗り | 将来拡張 |
|----|----------|------------|--------------|--------------|----------|
| **A. 人事から入る** | **1対1トレード**・案内文と同じブロック。**編成の一環**として説明しやすい。左メニュー増やさない。 | FA は「ロスターにいない相手」の操作で、**ロスター表だけ**だと発見補助が要る（ボタンラベルで吸収可）。 | **高** | **高**（`trade_fa_wrap` 隣） | オフ FA 全面化の前段としても伸ばしやすい。 |
| **B. 独立 FA ウィンドウ** | **発見性**が高い。一覧・フィルタの余地。 | 第1コミットが重くなりがち。**状態同期**（`season.free_agents`）の受け渡しが増える。 | **高** | **中** | 長期的にはあり得るが **最小導線の初手には重い**。 |
| **C. 当面 GUI 未対応＋案内のみ** | **実装ゼロ**で、誤った「CLI 正本」表現を **事実ベースに直せる**。リスク最小。 | プレイヤー価値は **説明の正しさ**に留まる。編成ギャップは残る。 | **中**（期待値調整） | **最高** | 後から A/B に差し替え可能。 |

---

## 4. 最小実装候補の比較

| 候補 | 小さく切れるか | プレイヤー価値 | 既存ロジック流用 | 今の段階に合うか |
|------|----------------|----------------|------------------|-------------------|
| **FA 候補一覧から 1 人契約（人事ボタン）** | **高**（`sign_free_agent`＋`can_team_afford`＋`can_team_sign_player_by_japan_rule`＋`inseason_roster_moves_unlocked`） | **高**（初めてユーザーがプール獲得を操作できる） | **高**（新規交渉式は不要） | **実装前に案内修正があると安全** |
| **GUI は入口だけ（実行は未接続）** | 最小だが **価値低**・誤解の元 | 低 | — | **非推奨** |
| **当面未対応を明示し別領域優先** | **最高** | 低（ただし **虚偽案内の除去**は価値あり） | — | **ギャップ開示だけなら最優先しうる** |
| **一覧表示のみ（契約ボタンなし）** | 中 | 中（情報価値） | 表示のみ | **契約まで行かないなら中途半端**になりやすい |

---

## 5. 推奨する最小方針

**結論（1 本）**: **二段に分ける。**

1. **先行（文書・文言・最小差分）**: 人事案内などで **「インシーズンFA は CLI 正本」**と書く部分を、**現状のメニュー構成と矛盾しない表現**に直す（**ユーザー向け FA プール契約は未実装、シーズン中の補強は CPU 自動＋ロックルール**等）。**これ単体で「GUI 化」ではないが、主導線の誠実さに必須**。
2. **続く最小 GUI（実装タスク）**: **人事から** `season.free_agents`（または同等リスト）を **一覧し 1 人選び**、`sign_free_agent` で獲得する **1 回完結ウィザード**（**1対1トレード**と同型の思想）。**金額スライダー・多段交渉・市場全面 UI はやらない**。

**今回やらない**: `conduct_free_agency` 改変、CPU 補強アルゴリズム変更、オフ FA のユーザー化、CLI に同等メニューを必ず追加する（**任意**。製品方針で後決めでよい）。

---

## 6. 最小 UI フロー案（ワイヤーなし）

前提: `inseason_roster_moves_unlocked` が True でない場合は **messagebox** で既存ロック文言（トレードと同様）。

1. **人事** → **「インシーズンFA（1人）」**等のボタン（ラベルは実装で調整）。
2. **FA プール**を `normalize` 済みリストで **Listbox / Treeview**（名前・位置・OVR・目安年俸程度）。
3. **1 人選択** → **確認**で `can_team_afford_free_agent` / `can_team_sign_player_by_japan_rule` / ロスター人数の **既存判定結果をテキスト表示**。
4. **実行** → `sign_free_agent(user_team, player)`。**キャンセル**で終了。

**今回やらない**: オファー額の自由入力、他チームとの競合シミュ、CPU FA との順序制御 UI。

---

## 7. 実装時の注意点

| 注意 | 内容 |
|------|------|
| **既存ロジック流用** | **`sign_free_agent` を正**にし、**二重実装**を増やさない。 |
| **GUI / CLI** | CLI に同機能を載せない判断も可。**文言は「GUI のみ」「未実装」のどちらかに合わせる**。 |
| **lock** | **`inseason_roster_moves_unlocked`** をトレードと同一視（`season_transaction_rules`）。 |
| **制限表示** | 国籍枠・キャップ room は **`can_team_sign_player_by_japan_rule` / `get_team_fa_signing_limit` / `can_team_afford_free_agent`** のメッセージを流用または短文化。 |
| **将来拡張** | 独立 FA ウィンドウに移すときも **`sign_free_agent` 呼び出しは1箇所**に集約しやすいよう、**薄いウィザード**に留める。 |

---

## 8. 次に切り出せる実装タスク候補（2 件以内）

### タスク 1: 人事案内の事実整合（文言のみ）

| 項目 | 内容 |
|------|------|
| **目的** | **インシーズンFA に関する「CLI 正本」表現**を、**`run_gm_menu` に FA 契約項目がない事実**と整合させる。 |
| **触る範囲** | `main_menu_view._format_hr_trade_fa_guidance_text`、必要なら関連ラベル1箇所。 |
| **触らない範囲** | `sign_free_agent`、CPU FA、トレードロジック。 |
| **完了条件** | 文面が **静的コードと矛盾しない**（プレイヤーが「どこを開けばよいか」迷子にならない）。 |

### タスク 2: 人事からのインシーズンFA 1人獲得ウィザード（最小）

| 項目 | 内容 |
|------|------|
| **目的** | **GUI のみ**で FA プールから **1 人**獲得できる（`sign_free_agent`）。 |
| **触る範囲** | `main_menu_view.py`（子 `Toplevel`）、必要なら `main.py` に **薄いヘルパ**（一覧取得・ロック再利用）。 |
| **触らない範囲** | `run_cpu_fa_market_cycle` のアルゴリズム、`conduct_free_agency`、交渉式の新設。 |
| **完了条件** | ロック時はブロック、**Yes でロスターに追加**、**smoke 通過**、手動で 1 回獲得確認。 |

---

## 9. 更新ルール

- **事実（メニュー・呼び出し関係）**が変わったら本書を更新する。  
- **実装は別タスク**。  
- **推測を断定にしない**（「CLI に隠しコマンドがある」等はコードで見つけたときだけ追記）。

---

## 変更履歴

- 2026-04-07: 初版。ユーザー向けインシーズンFA契約は **メニュー未実装**、CPU 経路と `sign_free_agent` 利用箇所を整理。推奨は **案内整合 → 人事1人FA** の二段。

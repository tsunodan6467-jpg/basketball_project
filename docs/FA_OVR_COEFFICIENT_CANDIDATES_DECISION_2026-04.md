# `GENERATOR_INITIAL_SALARY_BASE_PER_OVR` — 仮置き候補の絞り込み（決裁メモ）

**作成日**: 2026-04-08  
**性質**: **係数の仮候補決裁（コード変更なし）**。机上効き: `docs/FA_OVR_COEFFICIENT_EFFECT_NOTE_2026-04.md`。調整優先: `docs/FA_OVR_COEFFICIENT_VS_FLOOR_DECISION_2026-04.md`。帯の仮線引き: `docs/FA_OVR_THRESHOLD_DECISION_2026-04.md`。

---

## 1. 目的

- **`GENERATOR_INITIAL_SALARY_BASE_PER_OVR`** の **仮置き値**を **1〜2 個**に **絞る**（**最終確定・実装はしない**）。

---

## 2. 確定事実

- **現在値**は **`1_220_000`**。  
- **OVR 80 / 90** への **机上の `raw_linear` 変化**は **ケース A/B/C** で **`FA_OVR_COEFFICIENT_EFFECT_NOTE` に整理済み**。  
- **今回の意図**は **効きが強すぎず弱すぎない**帯に **仮候補を置く**こと。**実装値の確定はまだしない**。

---

## 3. 比較候補（A/B/C のみ）

### 候補A — `1_180_000`

- **OVR 80 / 90** で **基準比 約 −3.2M / −3.6M**。**効きは小さめ**（**安全寄り**）。

### 候補B — `1_150_000`

- **約 −5.6M / −6.3M**。**中くらい**（**変化が観測しやすい**）。

### 候補C — `1_100_000`

- **約 −9.6M / −10.8M**。**やや強め**（**初手としては下げ幅が大きめ**。**線形項のみで OVR 90 が約 99M**）。

---

## 4. 今回の判断（1 案）

**仮置き候補は `1_180_000` と `1_150_000` の 2 つに絞る。`1_100_000` は初手としては効きがやや強めなので、現時点では後順位に置く。**

- **1 本だけに絞る運用**にするなら **主候補は `1_150_000`** でもよい（**中間**）。  
- **断定・確定ではない**。**検証や方針で見直し可**。

---

## 5. 理由

- **`1_180_000`** は **安全寄り**で **副作用を見ながら試しやすい**。  
- **`1_150_000`** は **効きが確認しやすい中間**。  
- **`1_100_000`** は **初手の一歩としては下げ幅が大きめ**— **まずは安全側と中間の 2 本**を **持つ方が自然**。  
- **`legacy_floor`** は **まだ触らない**（**別決裁**）。

---

## 6. 非目的

- **コード変更**。  
- **最終値の確定**。  
- **`legacy_floor` 調整**への **移行**。  
- **budget 側へ議論を戻すこと**。  
- **実装手順の具体化**（**次メモ**）。

---

## 7. 次に続く実務（1つだけ）

**仮置き候補のうち主候補 1 つを選び、その値で試す実装方針メモを docs に1本作る。**

---

## 抽出コマンド（参照用）

```powershell
Set-Location c:\Users\tsuno\Desktop\basketball_project
Select-String -Path docs\FA_OVR_COEFFICIENT_CANDIDATES_DECISION_2026-04.md -Pattern "目的|確定事実|比較候補|今回の判断|理由|非目的|次に続く実務"
```

---

## 改訂履歴

- 2026-04-08: 初版（仮候補＝`1_180_000`・`1_150_000`。`1_100_000` は後順位）。

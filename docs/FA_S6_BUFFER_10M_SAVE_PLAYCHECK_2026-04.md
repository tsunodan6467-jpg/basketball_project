# S6：10M buffer 適用後の実セーブ観測メモ（`--save`）

**作成日**: 2026-04-06  
**文書の性質**: **観測メモ（コード変更なし）**。合成側の前提: `docs/FA_S6_BUFFER_10M_PLAYCHECK_2026-04.md`（クイック・既定）、`docs/FA_S6_BUFFER_10M_REALWORLD_PLAYCHECK_2026-04.md`（`--seasons 1`）いずれも **1920/1920 が `final_offer == 10_000_000`**。第二試行決裁: `docs/FA_S6_BUFFER_10M_DECISION_MEMO_2026-04.md`。観測スクリプト: `tools/fa_offer_real_distribution_observer.py`。

---

## 1. 文書の目的

**実セーブ**（実際のロスター・`payroll_budget`・FA プール）で、`tools/fa_offer_real_distribution_observer.py --save <path>` を実行し、**10M buffer 適用後の `final_offer == 10_000_000` 件数・比率**を記録する。合成観測では全面張り付きが続いているため、**ゲーム実体でも同型か**を切り分ける。

---

## 2. 観測条件

### 2.1 観測ステータス（本コミット時点）

| 項目 | 内容 |
|------|------|
| **`--save` 実行** | **未実施** |
| **理由** | Cursor 作業環境で **利用可能な `.sav` ファイルが見つからなかった**ため。 |

**探索した場所（ファイル名のみ列挙・パス詳細は不要な範囲）**

- リポジトリ `basketball_project` 配下（`**/*.sav`）  
- `Desktop` / `Documents` / `Saved Games` 配下の再帰検索（PowerShell、`ErrorAction SilentlyContinue`）  
- ユーザープロファイル直下の **深さ制限付き**再帰（`.sav` フィルタ）

いずれも **ヒット 0 件**。プレイデータが別ドライブ・Steam ユーザーデータ・OneDrive 等にある場合は、**手元でパスを指定して再実行**し、§3 の表を上書き記入すること。

### 2.2 再現用コマンド（セーブパスは差し替え）

スクリプト既定: **`--seed 42`**、**`--fa-cap 40`**、**`--seasons 0`**（`--save` 使用時は生成スキップ）。観測直前に **`_sync_payroll_budget_with_roster_payroll` ×2**（実装どおり）。

```text
Set-Location c:\Users\tsuno\Desktop\basketball_project
python -m basketball_sim --smoke
python tools\fa_offer_real_distribution_observer.py --save "C:\path\to\your\slot.sav"
```

**`final_offer == 10_000_000` 厳密件数**は stdout に無い場合があるため、必要なら `FA_S6_BUFFER_10M_PLAYCHECK` と同様 **`python -c`** で `load` → 同期2回 → `_run_matrix` 相当の集計を行う（リポジトリ改変なし）。

**補助（任意）**

```text
python tools\fa_offer_diagnostic_observer.py
```

---

## 3. 10M 張り付きの観測結果（実セーブ）

**本稿作成時点: `--save` 未実行のため、数値は未記入。実行後に以下を埋める。**

| 指標 | 件数 | 比率 | 備考 |
|------|------|------|------|
| 使用 `.sav` の識別 | — | — | 例: ファイル名・スロット番号のみ |
| **総サンプル**（team × FA） | **—** | — | |
| **`final_offer == 0`** | **—** | — | |
| **`soft_cap_early == True`（S1）** | **—** | — | |
| **`0 < final_offer <= 10_000_000`** | **—** | — | |
| **`final_offer == 10_000_000`** | **—** | — | **本メモの主目的** |
| **`soft_cap_early == False` かつ `0 < final_offer < 10_000_000`** | **—** | — | |
| **`final_offer > 10_000_000`** | **—** | — | |
| **高額 FA（`salary >= 50_000_000`）かつ `final == 10_000_000`** | **—** | — | |
| **D1 / D2 / D3（全サンプル）** | **—** | — | 各段の件数 |
| **代表ケース** | — | — | `final != 10M` があれば数件 |

---

## 4. 既存観測との比較

| 条件 | `final == 10_000_000` | 総サンプル（参考） |
|------|------------------------|-------------------|
| クイック・`--seasons 0` | **1920 / 1920** | 1920 |
| `--seasons 1`（seed 42, fa-cap 40） | **1920 / 1920** | 1920 |
| **実セーブ・本メモ** | **未測定** | — |

- **現時点の結論**: 実セーブが無いため、**「全面張り付きのまま」か「分布が開いた」かは未判定**。  
- **第三試行判断への効き**: 数値が埋まるまで **30M 級のコード実装は決裁メモなしでは進めない**方針としつつ、**実セーブで張り付き率が下がれば 10M 維持の余地**が出る。**全面張り付きが続けば**、合成観測と整合し **第三試行（決裁＋定数）の根拠が強化**される。

---

## 5. 今回の観測から分かること

- **判明したこと**: 本環境では **`.sav` が取得できず、実セーブ列はゼロ本**である。観測パイプライン自体は `load_world` 経由で動かせる想定だが、**入力ファイルが無いと実行不能**。  
- **未判明のこと（要フォロー）**: **10M が実プレイ分布でも天井か**、**高額 FA の張り付き率**、**D 別偏り**は **§3 実行後**に初めて言える。  
- **暫定**: 合成2条件がいずれも **1920/1920** のため、**実セーブで開くかが第三試行・据え置きの分岐点**。

---

## 6. 今回はまだやらないこと

- **`_calculate_offer` 本体の改造**  
- **floor 条件の変更**  
- **オフ手動 FA の全面再設計**  
- **低額例外ルールの本体追加**  
- **第三試行 buffer の無決裁コード実装**  
- **観測スクリプトの改修**  
- **generator / GUI / 経営収支のついで改修**

---

## 7. 次に実装で触るべき対象（1つだけ）

**次の一手**: **手元で実在する `.sav` のフルパスを確定し、`python tools\fa_offer_real_distribution_observer.py --save "<path>"` を1回実行する。得られた stdout と（必要なら）`final == 10_000_000` の厳密集計を **本メモ §2.1 の「未実施」を「実施済み」に差し替え、§3・§4・§5 を数値で更新したコミット**にまとめる。**

- **なぜその1手が今もっとも妥当か**  
  合成（クイック・`--seasons 1`）だけでは **10M 全面張り付きが再現**しており、**第三試行に進む前に実セーブ1本が最後の不足ピース**である。コードを触る前に **観測1本で分岐**できる。  

- **何はまだ残るか**  
  **§3 が「全面張り付き」に近い場合**の **`docs/` における 30M 級第三試行の決裁メモ**と承認後の **`offseason.py` 定数更新**。**§3 で分布が開く場合**は **10M 維持**と **クリップ以外の要因調査**が残る。**複数セーブ**での再現性確認も任意。

---

## 改訂履歴

- 2026-04-06: 初版（`.sav` 未検出のため観測ブロックを記録。手順・比較枠のみ）。
